version: "2.4"

services:
  web:
    build: .
    command: poetry run python3 manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/opt/app
    ports:
      - "8000:8000"
    environment:
      - SERVER_ROLE
      - DJANGO_SECRET_KEY
      - DJANGO_DEBUG
      - DATABASE_URL
